name: agrin_ai
description: AgriN AI — Regenerative Agricultural Intelligence for India and BRICS.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_animate: ^4.5.2
  go_router: ^14.8.1
  intl: ^0.19.0
  http: ^1.4.0
  image_picker: ^1.1.2
  flutter_tts: ^4.2.5
  speech_to_text: ^7.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
